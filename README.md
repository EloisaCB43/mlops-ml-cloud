# mlops-ml-cloud

Repositorio para el curso de Aprendizaje Automático en la nube

## Propósito:

Esto es solo una prueba, seguí todos los pasos del repositorio que nos compartiste profe

## Ahora creo mi branch tarea_01

Para poder hacer "Abre un Pull Request en GitHub desde tu rama hacia `main`" hice:

5. Flujo de trabajo diario

```bash
# Crea una rama para tu trabajo
git checkout -b feature/my-topic

# Realiza tus cambios, añade y commitea
git add -A
git commit -m "Add: tutorial for X"

# Actualiza tu rama con los últimos cambios de main
git fetch upstream
git rebase upstream/main

# Haz push a tu fork
git push -u origin feature/my-topic
```
